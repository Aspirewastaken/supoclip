# Grafana Dashboard Provisioning Configuration

apiVersion: 1

providers:
  - name: 'SupoClip Dashboards'
    orgId: 1
    folder: 'SupoClip'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
